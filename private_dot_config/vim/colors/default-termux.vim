" Normally, we would load a color scheme and then modify it, but
" loading the default theme seems to reset 'background', which I don't
" want, so this is commented out:
" runtime colors/default.vim

let g:color_scheme='default-accessible'

hi Constant ctermfg=4
